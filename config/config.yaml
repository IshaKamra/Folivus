data:
  raw_dir: "data/raw/PlantVillage"
  img_size: 224
  batch_size: 64
  num_workers: 4
  train_split: 0.8
  val_split: 0.1
  test_split: 0.1

model:
  num_classes: 15
  dropout_rate: 0.5

training:
  epochs: 15
  lr: 0.001
  device: "cpu"
  model_save_path: "saved_models/folivus_net.pth"